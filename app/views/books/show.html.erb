<% if @book.errors %>
  <ul>
<% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
  </ul>
  <% end %>


    <div class="container">
    <div class="row">
    <div class="col-md-3">
  <h2>User info</h2>
   <%= attachment_image_tag @book.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" ,format: 'no_image.jpg' %>



  <h5>name</h5>
   <%= @book.user.name %>
  <h5>introduction</h5>
   <%= @book.user.introduction %>
   <%= link_to"",edit_user_path(@book.user.id), class: "btn btn-outline-secondary btn btn-block fas fa-user-cog"%>


<h2 class="mt-3">New book</h2>
    <%= form_for(@book_new,  url: books_path)  do |f| %>

  <lavel>Title</lavel>
  <%= f.text_field :title,class: "col-xs-12" %>

  <lavel>Opinion</lavel>
  <%= f.text_field :body,class: "col-xs-12" %>
  <div class="form-group">
     <%= f.submit 'Create Book',class: "col-xs-12 btn btn-success" %>
  <% end %>
  </div>

 </div>
  <div class="col-md-8 offset-md-1">
   <h2>Book detail</h2>

  <%= link_to user_path(@book.user_id) do %>
  <%= attachment_image_tag @book.user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg",size:'50x50',format: 'no_image.jpg' %>

  <%= @book.user.name %>
  </nav>
  <% end %>

  <td><%= link_to @book.title %></td>
  <td><%= @book.body %></td>
  <% if @book.user_id == current_user.id %>
  <td><%= link_to"Edit", edit_book_path(@book.id),class:"btn-sm btn-success" %></td>
  <td><%= link_to "Destroy", book_path(@book.id), class:"btn-sm btn-danger destroy(@book.id)", method: :delete,data: {confirm: "本当に消しますか？"} %></td>
  <% end %>
  </div>
</div>
</div>